# ============================================
# ODOO ENVIRONMENT VARIABLES TEMPLATE
# ============================================
# Copia questo file in .env e modifica i valori
# Il file .env è in .gitignore e non viene committato
#
# Per Docker Compose: le variabili qui vengono caricate automaticamente
# Per Dokploy: configura queste variabili nella dashboard
# ============================================

# Odoo Version (per riferimento, non usato direttamente)
ODOO_VERSION=19

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Configurazione PostgreSQL (usata da docker-compose per il servizio db)
POSTGRES_DB=odoo
POSTGRES_USER=odoo
POSTGRES_PASSWORD=change_this_password

# Configurazione database per Odoo (usata per generare odoo.conf)
DB_HOST=db
DB_PORT=5432
DB_USER=odoo
DB_PASSWORD=change_this_password
# DB_NAME: Lascia vuoto per multi-database, oppure specifica un nome per installazione singola
# DB_NAME=odoo

# ============================================
# ODOO CONFIGURATION
# ============================================
# Password per operazioni sul database (obbligatoria)
ADMIN_PASSWD=change_this_admin_password

# Dominio per installazione singola (es: odoo.victorianmonkey.org)
# Se impostato, configura dbfilter e list_db=False
# Se non impostato, usa configurazione multi-database
ODOO_DOMAIN=odoo.victorianmonkey.org

# Porta HTTP (default: 8069)
HTTP_PORT=8069

# Livello di log (debug, debug_sql, info, warn, error, critical)
LOG_LEVEL=info

# Numero di worker (raccomandato: CPU cores * 2 + 1)
WORKERS=2

# Abilita proxy mode quando dietro reverse proxy/Cloudflare (True/False)
PROXY_MODE=True

# ============================================
# OPTIONAL EMAIL CONFIGURATION
# ============================================
# Decommenta e configura se vuoi usare email
# EMAIL_FROM=odoo@yourdomain.com
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SSL=True
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-email-password

# ============================================
# OPTIONAL ADVANCED SETTINGS
# ============================================
# Database connection pool (default: 64)
# DB_MAXCONN=64

# ============================================
# NOTES
# ============================================
# - Cambia tutte le password di default prima di usare in produzione
# - DB_PASSWORD e POSTGRES_PASSWORD devono corrispondere
# - ODOO_DOMAIN deve corrispondere al dominio configurato nel reverse proxy
# - Per Dokploy: configura queste variabili nella dashboard del progetto